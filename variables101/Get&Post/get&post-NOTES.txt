/** NOTES
 *  $_GET, $_POST = is a "special variable" used to collect data from an HTML form 
 *                  data is dent to the file in the action attribute of <form>
 *                  <form action = "some_file.php" method = "get">
 * 
 *  $_GET = Data is appended to the url
 *          NOT SECURE
 *          char limits
 *        - "Bookmark is possible w/ values"
 *          GET requests can be cached
 *        - "Better for a search page"
 * 
 *  $_POST = Data is packaged inside the body of tyhe HTTP request
 *         - "MORE SECURE"
 *           Connot bookmark
 *           GET requests are not cached
 *         - "Better for submitting credentials"
 * 
 */